module Demo where

----------------------------------------------------------------------------------------------------------------
lastElem :: [a] -> a
lastElem = foldl1 seq
----------------------------------------------------------------------------------------------------------------
revRange :: (Char,Char) -> [Char]
revRange = unfoldr g 
  where g = (\(x,y) -> if y >= x then Just (y,(x,pred y)) else Nothing)
----------------------------------------------------------------------------------------------------------------

